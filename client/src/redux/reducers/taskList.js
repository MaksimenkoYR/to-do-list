const initialState = {
    complete: [],
    incomplete: [],
}

const taskList = (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state
        }
    }
}

export default taskList
